// 6 -> array method -> sort()
// w.a.p to arrange the array in ascending order.
arr = [10,4,6,13,2,1]
arr.sort((n1 , n2) => n1 - n2 )
console.log(arr);

//w.a.p to arrange the array in descending order.
arr.sort((x,y) => y-x)
console.log(arr);
